{
  "name": "prueba-tecnica",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "yarn install",
    "serve-backend": "yarn workspace backend serve",
    "serve-frontend": "yarn workspace frontend serve",
    "serve-all": "concurrently \"yarn serve-backend\" \"yarn serve-frontend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
